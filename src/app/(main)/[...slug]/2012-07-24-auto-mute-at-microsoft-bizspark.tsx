import { Typo } from '@/components';
import { APP_NAME } from '@/config';

export const categories = ['news', 'software'];
export const tags = ['software', 'pc', 'microsoft', 'windows'];
export const title = 'Auto Mute at Microsoft® BizSpark®';
export const content = (
  <>
    <Typo variant="paragraph">
      <strong>{APP_NAME}</strong> become a member of the <strong>Microsoft BizSpark</strong> partners program!
    </Typo>
  </>
);
